# Phase 05 — i18n, Build, Verify & Regression

## Context Links
- Plan: [plan.md](plan.md) | Depends on: Phase 03, 04
- Scripts (verified `package.json`): `lingui:prepare` (=`extract`+`compile`, l.28), `tscheck` (l.30), `lint` (l.21), `test`/`vitest` (l.19), `pre-push` (=`vitest run`).

## Overview
- Priority: P1
- Status: pending
- Finalize: extract+compile i18n catalogs for all new `t`/`<Trans>` strings, pass typecheck + lint, run unit tests, and execute a manual QA + FUNDED regression checklist. No new features.

## Key Insights
- Lingui auto-extracts from `t`/`<Trans>` macros across `src` → run `yarn lingui:prepare`. New strings land untranslated (English source) in all catalogs — acceptable; translations follow later.
- No web build command needed for verification beyond `tscheck` (full app build is heavy + sandbox-blocked). `tscheck` + `lint` + `vitest` gate correctness.
- Optional: add focused unit tests for pure logic (`generateTraitsFromOnchainData`, eligibility `buildResult`, countdown formatting, `getNftMintNewsletterContext`) — high value, deterministic, no chain. Mirror `funded-utils.spec.ts` style.

## Requirements
Functional: all NFT strings extracted/compiled; flag-on and flag-off behavior verified; FUNDED feature unaffected.
Non-functional: `tscheck` clean, `lint:ci` 0 warnings on new files, `vitest run` green.

## Architecture
Verification flow: lingui extract → compile → tscheck → lint → vitest → manual QA matrix (flag on/off, both pages, wallet states, chain switch) → FUNDED regression.

## Related Code Files
Create (optional tests):
- `src/domain/nft/nft-traits.spec.ts` — deterministic trait gen for a fixed seed.
- `src/domain/nft/nft-eligibility.spec.ts` — `buildResult` for volume {0, 125k, 250k, 300k} → mints {0,1,2,2}.
- `src/domain/nft/nft-mint-newsletter-context.spec.ts` — gmx id → expected context.
Modify: locale catalogs under `src/locales/*/messages.po`+`.js` (regenerated by lingui).

## Implementation Steps
1. `yarn lingui:prepare` → verify new NFT strings appear in `src/locales/en/messages.po`; commit catalogs.
2. `yarn tscheck` → fix any type errors across phases 1-4.
3. `yarn lint` (autofix) then `yarn lint:ci` on new files → 0 warnings.
4. (Optional) write the 3 spec files; `yarn test:ci` → green.
5. Manual QA matrix (below).
6. FUNDED regression (below).
7. Docs: note feature + env vars in `docs/` if impact (state Docs impact).

## Test Matrix
| Area | Type | Check |
|------|------|-------|
| Trait gen | unit | fixed seed → stable traits |
| Eligibility buildResult | unit | volume thresholds → qualifiedMints/waves/percent |
| Newsletter context | unit | gmx id → `gmx_nft_mint` (or confirmed value) |
| Flag off | manual/e2e | `/nft`+`/nft/eligibility` → PageNotFound; nav hidden |
| Flag on, no wallet | manual | pages render; actions show "connect wallet" hints |
| Flag on, wallet (Arbitrum NFTs) | manual | V2+V3 grid loads; staked NFT glows |
| Stake/unstake | manual | chain switch → tx → success dialog → list refresh |
| Merge (5 Bronze) | manual | warning → tx → Silver success |
| Migrate (V2) | manual | approve → migrate → success |
| Eligibility check | manual | valid addr → volume + waves + countdown + progress |
| Email reminder | manual | valid email → subscribe success state |
| Dialog adapter | manual | every dialog opens/closes, ESC works, scroll locked |

## Manual QA Checklist
- [ ] Flag off: both routes → PageNotFound; NFT nav item absent.
- [ ] Flag on: nav "NFT" w/ Management + Mint Eligibility sub-items; collapsed sidebar shows single icon.
- [ ] Icon `ic_star_gradient` distinct from FUNDED `ic_star`.
- [ ] `/nft` sections render; responsive grid 2-6 cols.
- [ ] Stake/unstake/merge/migrate happy paths on Arbitrum.
- [ ] OpenSea buy link opens correct collection.
- [ ] `/nft/eligibility` check flow + countdown ticking + progress bar.
- [ ] No console errors; no `clsx`/`lucide-react`/`next/*` import errors.

## FUNDED Regression Checklist
- [ ] FUNDED nav item + 3 sub-items still render (`FUNDED_ENABLED`).
- [ ] `/funded/start-journey`, `/funded/my-journeys`, `/funded/challenge-dashboard` unchanged.
- [ ] SideNav order/spacing intact (NFT added after FUNDED, `SideNav.tsx:205`).
- [ ] No route collisions (`/nft` vs any funded/trade path).
- [ ] `tscheck` passes for both `tsconfig.json` and `landing/tsconfig.json`.

## Success Criteria
- `yarn lingui:prepare`, `yarn tscheck`, `yarn lint:ci`, `yarn test:ci` all pass.
- Manual QA matrix passes; FUNDED regression clean.

## Risk Assessment
| Risk | L×I | Mitigation |
|------|-----|------------|
| Lingui extract pollutes catalogs / breaks compile | Low×Med | Run `lingui:prepare`; inspect diff; only English source added. |
| tscheck fails on viem/wagmi type gaps | Med×Med | Fix incrementally; ABIs typed as `unknown`→cast like reference. |
| Manual chain-switch QA needs funded Arbitrum wallet | Med×Low | Use a wallet holding test/real Foxify NFTs; or verify reads only if none. |
| Hidden regression in SideNav render | Low×High | Snapshot/visual compare before/after; FUNDED checklist. |

## Security Considerations
- Confirm no secrets committed: grep `1080fc62`, raw OpenSea/merkle keys → 0. `.env` not committed (only `.env.example` placeholders).
- Verify `VITE_*` env inlining exposes only intended public values.

## Next Steps
- Resolve Unresolved Questions (frontendId, newsletter context, wave dates, mint enablement) with user before production enable.
- Translations for new strings (separate task).
